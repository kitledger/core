# Proxy functions (API implementations) need to:

* Serialize/Deserialize the request and the responses
* Perform data validation, ideally using Valibot
* Perform permission checks
* Handle errors gracefully